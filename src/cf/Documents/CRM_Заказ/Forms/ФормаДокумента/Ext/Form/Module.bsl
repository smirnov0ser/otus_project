
&НаСервере
Процедура ОплатитьНаСервере()
	Если Объект.Статус = Перечисления.CRM_СтатусыЗаказов.Новый Тогда
		Объект.Статус = Перечисления.CRM_СтатусыЗаказов.Оплачен;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Оплатить(Команда)
	ОплатитьНаСервере();
КонецПроцедуры 


